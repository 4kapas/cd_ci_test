name: CI-CD

on:
  push:
    branches: [main]

jobs:
  deploy:
    runs-on: self-hosted

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Trigger deploy.bat on my local PC
        run: curl.exe -X POST http://localhost:3000/deploy
