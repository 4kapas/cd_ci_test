import styled from "styled-components";

export const StyledDeleteDatasetModal = styled.div`
  & > p {
    font-size: 16px;
    color: var(--gray-1000);
    text-align: center;
    & > span {
      font-weight: 900;
      display: block;
    }
  }
`;
